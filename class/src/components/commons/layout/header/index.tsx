import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 50px;
  background-color: skyblue;
`;

export default function LayoutHeader() {
  return <Wrapper>여기는 헤더 영역입니다</Wrapper>;
}
